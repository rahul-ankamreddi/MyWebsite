-- some secret game
- text color white makes the text invisible which can be read while highlighting it with cursor. By clicking this text, it will redirect to other page(secret page).

-- link to the forums

-- where they can leave a message